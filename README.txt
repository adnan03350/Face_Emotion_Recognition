1. first open the app in vs code and access the project folder
2. open new terminal and create virtual environment like this 
python -m venv myenv
.\nyenv\Scripts\Activate
pip install -r requirements.txt

3.when succefully install all necessary libraries then run the code and enjoy.